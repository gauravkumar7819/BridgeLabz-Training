using System;
class AreaOfCircle
{
    static void Main()
    {
        double radius = 7;

        double area = Math.PI * radius * radius;

        Console.WriteLine("Area of Circle is: " + area);
    }
}